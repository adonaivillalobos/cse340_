<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= vehicle.inv_make %> <%= vehicle.inv_model %></title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <main class="vehicle-detail">
        <h1><%= vehicle.inv_year %> <%= vehicle.inv_make %> <%= vehicle.inv_model %></h1>
        <section class="vehicle-container">
            <img src="<%= vehicle.inv_image %>" alt="Image of <%= vehicle.inv_make %> <%= vehicle.inv_model %>">
            <div class="vehicle-info">
                <p><strong>Price:</strong> <span>$<%= vehicle.inv_price.toLocaleString("en-US") %></span></p>
                <p><strong>Mileage:</strong> <%= vehicle.inv_miles.toLocaleString("en-US") %> miles</p>
                <p><strong>Color:</strong> <%= vehicle.inv_color %></p>
                <p><strong>Description:</strong> <%= vehicle.inv_description %></p>
            </div>
        </section>
    </main>
</body>
</html>
